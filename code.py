# Launches the Adventure-Engine shell
#
# This is done in a sperate file so that __name__ is set to "advent" instead of "__main__"
# which helps some modules detect that they are running in advent.

import traveler
